<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/ui_background"
    android:orientation="vertical">

    <!-- Immersive Header -->
    <LinearLayout
        android:id="@+id/header_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:minHeight="?attr/actionBarSize"
        android:background="@color/ui_header_background"
        android:fitsSystemWindows="true"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingStart="16dp"
        android:paddingEnd="16dp">

        <ImageView
            android:id="@+id/btn_back"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:background="?selectableItemBackgroundBorderless"
            android:clickable="true"
            android:focusable="true"
            android:src="@drawable/ic_arrow_back"
            android:tint="@color/ui_header_text" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="?attr/actionBarSize"
            android:layout_marginStart="32dp"
            android:gravity="center_vertical"
            android:text="@string/header_gestures"
            android:textColor="@color/ui_header_text"
            android:textSize="20sp"
            android:textStyle="normal" />
    </LinearLayout>

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingBottom="42dp">

            <!-- ================= SENSITIVITY SETTINGS ================= -->
            <LinearLayout
                android:id="@+id/section_sensitivity"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:background="@color/ui_item_background"
                android:layout_marginStart="12dp"
                android:layout_marginEnd="12dp"
                android:layout_marginTop="12dp"
                android:layout_marginBottom="4dp"
                android:paddingStart="16dp"
                android:paddingEnd="16dp"
                android:paddingTop="12dp"
                android:paddingBottom="16dp">

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@string/sensitivity_settings"
                    android:textColor="@color/ui_icon_bg"
                    android:textSize="13sp"
                    android:textStyle="bold"
                    android:layout_marginBottom="12dp" />

                <!-- Edge Threshold -->
                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="horizontal"
                    android:gravity="center_vertical"
                    android:layout_marginBottom="4dp">
                    <TextView
                        android:layout_width="0dp"
                        android:layout_height="wrap_content"
                        android:layout_weight="1"
                        android:text="@string/edge_threshold_label"
                        android:textColor="@color/ui_text_primary"
                        android:textSize="14sp" />
                    <TextView
                        android:id="@+id/tv_edge_threshold_value"
                        android:layout_width="48dp"
                        android:layout_height="wrap_content"
                        android:gravity="end"
                        android:textColor="@color/ui_text_secondary"
                        android:textSize="13sp" />
                </LinearLayout>
                <SeekBar
                    android:id="@+id/seekbar_edge_threshold"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginBottom="12dp"
                    android:max="18"
                    android:min="2" />

                <!-- Swipe Threshold -->
                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="horizontal"
                    android:gravity="center_vertical"
                    android:layout_marginBottom="4dp">
                    <TextView
                        android:layout_width="0dp"
                        android:layout_height="wrap_content"
                        android:layout_weight="1"
                        android:text="@string/swipe_threshold_label"
                        android:textColor="@color/ui_text_primary"
                        android:textSize="14sp" />
                    <TextView
                        android:id="@+id/tv_swipe_threshold_value"
                        android:layout_width="56dp"
                        android:layout_height="wrap_content"
                        android:gravity="end"
                        android:textColor="@color/ui_text_secondary"
                        android:textSize="13sp" />
                </LinearLayout>
                <SeekBar
                    android:id="@+id/seekbar_swipe_threshold"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:max="130"
                    android:min="20" />

            </LinearLayout>

            <!-- ================= LEFT TOP ================= -->
            <include layout="@layout/item_gesture_zone_left"
                android:id="@+id/zone_left_top" />

            <!-- ================= LEFT MID ================= -->
            <include layout="@layout/item_gesture_zone_left"
                android:id="@+id/zone_left_mid" />

            <!-- ================= LEFT BOTTOM ================= -->
            <include layout="@layout/item_gesture_zone_left"
                android:id="@+id/zone_left_bottom" />

            <!-- ================= RIGHT TOP ================= -->
            <include layout="@layout/item_gesture_zone_right"
                android:id="@+id/zone_right_top" />

            <!-- ================= RIGHT MID ================= -->
            <include layout="@layout/item_gesture_zone_right"
                android:id="@+id/zone_right_mid" />

            <!-- ================= RIGHT BOTTOM ================= -->
            <include layout="@layout/item_gesture_zone_right"
                android:id="@+id/zone_right_bottom" />

        </LinearLayout>
    </ScrollView>
</LinearLayout>
