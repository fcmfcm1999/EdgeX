# Copilot Instructions

## Git Commits
- **Do not create git commits automatically after making code changes.**
- Only run `git commit` when the user explicitly asks to commit (e.g. "提交", "commit", "可以提交").
